<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.dudkovlad.ymdsstars"> <!-- Yandex mobile developers school - stars -->

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:fullBackupContent="true"
        android:name=".MyApp"
        android:icon="@drawable/ic_face"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AndroidTheme">


        <activity
            android:name=".actors_activity.ActorsRVActivity"
            android:theme="@style/AndroidTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".actor_activity.ActorActivity"
            android:theme="@style/SlidingTheme">

        </activity>

    </application>

</manifest>
